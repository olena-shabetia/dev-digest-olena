/** Drawer width (px). */
export const DRAWER_WIDTH = 560;

/** Accepted file extensions for the file input — server parses `.md`/
 *  `.markdown` directly, and picks the skill core out of a `.zip`. */
export const ACCEPT = ".md,.markdown,.zip";
