/** Constants for SkillCard. */

/** Skill type → icon-box colour. Every `SkillType` is covered, no fallback needed. */
export const TYPE_COLOR: Record<string, string> = {
  rubric: "#3b82f6",
  convention: "#10b981",
  security: "#ef4444",
  custom: "#8b5cf6",
};
