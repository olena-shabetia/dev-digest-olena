/** Minimum pointer movement (px) before a drag starts — keeps a plain click on
 *  the handle (or a mis-aimed click near it) from being interpreted as a
 *  drag, matching the dnd-kit "activation constraint" pattern. */
export const DRAG_ACTIVATION_DISTANCE = 4;
